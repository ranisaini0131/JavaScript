'use strict'; //activated , avoid accidental errors, helps to introduce bugs, save us from silent errors

let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriversLicense = true;
if (hasDriversLicense) console.log("I can drive");

// const interface = "Audio"// error reserved keyword
